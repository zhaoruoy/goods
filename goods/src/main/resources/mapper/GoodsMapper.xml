<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goods.mapper.GoodsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.goods.entity.Goods">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="goodsType" property="goodstype" />
        <result column="count" property="count" />
        <result column="remark" property="remark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, goodsType, count, remark
    </sql>

    <select id="pageCC" resultType="com.goods.entity.Goods">
        select * from goods ${ew.customSqlSegment}
    </select>
    <select id="getGoods" resultType="com.goods.entity.Goods">
        select * from goods where goodsType=#{id}
    </select>
</mapper>
